export interface ArrowProps {
  /**
   * The width of the arrow in pixels.
   *
   * @defaultValue 10
   */
  width?: number
  /**
   * The height of the arrow in pixels.
   *
   * @defaultValue 5
   */
  height?: number
}
